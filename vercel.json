{
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 300
    }
  },
  "env": {
    "PRISMA_GENERATE_DATAPROXY": "true"
  },
  "buildCommand": "npx prisma generate && next build",
  "outputDirectory": ".next"
}